import TabPanel from 'components/TabPanel';
import styled from 'styled-components';


interface Props { }
const RegisterTabPanel = styled(TabPanel) <Props>`
  &>div {
    padding: 0px;
  }
`;

export default RegisterTabPanel
